@model FinalProject.Oggetto

@{
    ViewBag.Title = "Edit";
}

<h2>Edit</h2>


@using (Html.BeginForm())
{
    @Html.AntiForgeryToken()
    
<div class="form-horizontal">
    <h4>Oggetto</h4>
    <hr />
    @Html.ValidationSummary(true, "", new { @class = "text-danger" })
    @Html.HiddenFor(model => model.idOggetto)
    <div class="container">
        <div class="row m-3">
            <div class="col-3"></div>
            <div class="col-6">
                <div class="form-group text-center">
                    <div class="form-floating row">
                        @Html.EditorFor(model => model.Oggetto1, new { htmlAttributes = new { @class = "form-control row", @placeholder = "Oggetto" } })
                        <label for="oggetto"> Oggetto</label>
                        @Html.ValidationMessageFor(model => model.Oggetto1, "", new { @class = "text-danger" })
                    </div>
                    <div class="row m-3">
                        <input type="submit" value="Conferma" class="btn btn-outline-success row" />
                    </div>
                </div>
            </div>
            <div class="col-3"></div>
        </div>
        
    </div>
</div>
}

@section Scripts {
    @Scripts.Render("~/bundles/jqueryval")
}
